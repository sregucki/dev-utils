#!/bin/bash

alias lla="gls -Flhao --group-directories-first --color=auto"
alias buu="brew update && brew upgrade && brew cleanup && brew autoremove"
alias dpsf='. $HOME/IdeaProjects/dev-utils//aliases/docker-ps-rancher.sh'